services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ikphotos
      POSTGRES_USER: ik
      POSTGRES_PASSWORD: ik
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
